name: todo_backend
description: A new Dart Frog backend project for the Todo App.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.0.0
  postgres: ^3.0.0
  json_annotation: ^4.8.1
  equatable: ^2.0.5

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  mocktail: ^1.0.0
  test: ^1.24.0
